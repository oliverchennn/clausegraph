# ClauseGraph current checkpoint

## Merged application checkpoint: 2026-09-20 UTC

The user confirms other developers are inactive and authorizes this agent to finish all required reviews, fixes, implementation and green sequential merges alone. Reviews are recorded as same-agent orchestration. Existing ownership lanes, fresh worktrees, financial/privacy invariants and full CI remain in force. See the current [AGENTS override](../AGENTS.md).

Current merged main is `f85aa08bc127d2900d26345963b5a1421a6294fd` (PR40). Cash-gap, C10/C11, uncertainty controls and C12's accessible failure view are delivered. PR39 (`96b220e`) reviewed the [separate synthesis design](RESILIENT_PLAN_SPEC.md); PR40 CI 35499343638 passed 390 backend and 51 browser tests. The stale audit closed duplicate PR38 after preserving its useful retry regression in PR36; preserve historical branches and handoffs.

The active [resilient engine task](handoffs/dev-a/resilient-plan-engine.md) adds nonmutating bounded search, fresh atomic adoption and a separate three-source synthetic success fixture. It distinguishes first-feasible FOUND, exhaustively refuted NO_SOLUTION and bounded/unresolved INCONCLUSIVE. The original six-source demo remains unchanged. After engine/contracts merge, finish comparison/adoption/C13, then consequence contract/integration/C14 and C15 final demo/fallback. Optional #5/#6 are not activated. Successful live inference and human spoken rehearsal remain separately outstanding; do not invent them or block independent local work.

The [delivery record](DELIVERY_CHECKPOINT.md) verifies exact merged-main checkpoint `2e76537`: 390 backend tests including PostgreSQL and 46 real-API browser tests passed, alongside Ruff, contract drift, frontend checks and clean installs on Linux/Windows/macOS. The earlier `ca7cde5` and `a00af57` checkpoints retain their historical test counts. Existing worktrees and the user's running app were preserved, so the running app's version is not inferred from Git HEAD.

Read [WORKSTREAMS.md](WORKSTREAMS.md) for current PR heads, ownership and integration order. C's historical PR16/17 exceptions remain historical; new C10-C15 authorization is the exact-path delegation in this reassignment, within the existing dev-b lane. C's required deliverables count toward stage completion; its extra review does not replace required A/B mutual review. Peer handoffs remain unchanged.

[NVIDIA_BREV.md](NVIDIA_BREV.md) explains hosted/private text inference. Hosted NVIDIA remains the configured default; Brev browser consent is merged in PR28 (`22f6a2c`). Its simulated browser route tests do not establish live inference. The [historical Brev retest](handoffs/dev-a/brev-live-retest.md) passed 3/5 then 5/5 exact fields with unchanged code; citations/unreviewed withholding passed 5/5 both times. This is observed variability, not an improvement or general accuracy/OCR/full-browser result. No provider call occurs in the current contract specification task.

A's merged [live-demo-closeout handoff](handoffs/dev-a/live-demo-closeout.md) records earlier preparation: 130 focused passes/eight PostgreSQL skips, reproduced offline reports, prepared native/scanned fixtures and no-consent upload/deletion checks with zero provider requests. Its pending-consent statement is historical: the user subsequently authorized both hosted NVIDIA fixtures. Native extraction timed out after 120.250 seconds; OCR returned HTTP 503 after 0.250 seconds; both failed without rules. PR30 (`5e79c1d`) merges the [follow-up](handoffs/dev-a/incomplete-source-guard.md): incomplete sources keep saved/preview plans unresolved and verification returns HTTP 409 before computation/history persistence. The handoff records 348 backend passes/nine PostgreSQL skips and 28 real-API browser passes, with simulated inference. Successful live extraction, actual human review and spoken rehearsal remain outstanding. Preserve the granted consent scope without requesting it again or extending it to other destinations.

## Existing baseline

Source/evidence/condition review, nominal planning, source-to-cash decision traces, nonmutating scenario previews and bounded fixed-plan verification are already implemented. Original synthetic values remain baseline-40000/50000 cents, approved shift5000/50000, denied shift-40000/50000, cancellation alone-82000/8000. No money is computed by language models or React. See [verification semantics](handoffs/verification.md), [integration history](handoffs/integration.md), [architecture](ARCHITECTURE.md) and [demo](DEMO.md).

[Historical CI on checkpoint `a00af57`](https://github.com/oliverchennn/clausegraph/actions/runs/35486912817) passed 352 backend tests including PostgreSQL 17, 11 real-API Chromium tests, Ruff, generated OpenAPI/TypeScript drift, frontend typecheck/lint/build, and clean installs on Linux, Windows and macOS. This checks PR20 and PR21 together. A's earlier readiness-task history/uncertainty checks passed 45 tests with eight PostgreSQL variants skipped because no local disposable test URL is configured. Both offline scripts passed and their saved JSON reloaded with the expected outcomes.

The merged [history contract](HISTORY_CONTRACT.md) documents private history, assumptions, stable ordering and deletion beyond the displayed 30. The [uncertainty contract](UNCERTAINTY_CONTRACT.md) and [explorer contract](UNCERTAINTY_EXPLORER.md) document bounded semantics, exact counts and proof-qualified results. Earlier [integration](handoffs/dev-a/integration-validation.md), [history](handoffs/dev-a/history-contracts.md) and [uncertainty](handoffs/dev-a/uncertainty-limits.md) handoffs retain their original checks. Cash-gap API/UI/C11 and richer uncertainty controls are implemented. C12 is merged. Synthesis UI and consequence walkthrough remain assigned work after the active backend contract.

## Run/check

Use Python3.12, Node22.23.2/npm10.9.8. Install backend with backend/requirements.txt constrained by backend/requirements.lock; frontend with npm ci. After bootstrap merges, activate the Python environment and run python scripts/install_hooks.py in each clone. Existing custom hooks are preserved; inspect/chaining is needed if core.hooksPath is configured. Windows local Python may require .venv/Scripts/python.exe; macOS/Linux .venv/bin/python.

Run python scripts/dev.py for the local stack. Do not duplicate running ports or run frontend dev/build/E2E against the same .next directory concurrently. Lane browser tests use distinct E2E_API_PORT/E2E_WEB_PORT and their own checkout. Full checks: backend pytest including PostgreSQL in CI, Ruff, OpenAPI/types drift, frontend typecheck/lint/build, browser flows, and Linux/Windows/macOS clean installs.

## Remaining limits

The historical PR8 queue failures are resolved; PR17's historical ancestry-gate failure remains recorded in the integration handoff. B's [merged task 5 handoff](handoffs/dev-b/demo-rehearsal.md) now records a 180.02-second automated operator rehearsal, local fallback/outage checks and focused desktop/mobile sign-off. A's PR21 review found no blockers and independently passed 11 browser tests. Human spoken delivery remains unmeasured, and no global feature freeze is declared. History is now merged and its combined checkpoint is verified in the delivery record; stage 0 preserves the recorded fallback while live processing and human rehearsal remain outstanding.

Hosted NVIDIA evidence/OCR, audio, Tiger Data, cloud storage and deployment remain live-unverified. The limited historical Brev native-text results above do not establish them. The two hosted attempts have explicit consent and recorded failures, not successful inference. Further live checks must preserve destination and processing-consent scope. No billing/provisioning or public real-data release is authorized by this increment. Keep provider failures visible and source/session deletion semantics intact.

The private repository's current plan cannot require checks before a GitHub merge; hooks/CI reduce accidental conflicts but A must follow the review/green-check rule. No workflow guarantees freedom from semantic conflicts. No force-push, discarded changes, public visibility change or account upgrade is needed.

Keep the recorded fallback stable during the [required expansion stages](HACKATHON_ASSIGNMENTS.md). The earlier freeze/deferred-feature order is superseded by this explicit user assignment. Synthesis design review is recorded under the solo authorization; correlations, uncertain expenses and adaptive schedules remain outside v1. Optional #5/#6 never displace required delivery or human rehearsal. The active task is backend implementation with generated contracts; frontend follows its merge. No provider call, user-service restart or deployment occurred.
