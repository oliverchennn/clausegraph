# A merge coordination: uncertainty controls

The user requested checking current PR usefulness and resolving/merging useful branches. PR35's controls are new: main only has the original payday/approval form. Original head `0752aaada32944f865b3977d0474a901039733dd`; reviewed main before resolution `c82319c9b25bd2f2662290cada614df76e4b38d3`. Isolated checkout `.worktrees/dev-a-merge-uncertainty-ui`. Preserve B/C's original handoff; this is A's separate integration record.

Resolve duplicate cash-panel ancestry to the reviewed PR34 implementation, CSS and existing regressions. Keep only the new uncertainty controls/helpers/types/tests and C12's unrendered typed seam; the existing counterexample remains available. C12's future view is still undelivered and is not implemented here. PR36's focused cash demo must merge before this PR; adapt its selectors after that prerequisite is on main.

Allowed integration paths: `frontend/src/components/verify-plan.tsx`, `uncertainty-controls.tsx`, `cash-gap-diagnostic.tsx`; `frontend/src/lib/types.ts`, `uncertainty.ts`; `frontend/src/app/globals.css`; `frontend/tests/uncertainty.spec.ts`, `verification.spec.ts`, `cash-gap.spec.ts`, `cash-gap-demo.spec.ts`; this handoff. Remove the superseded `cash-gap-panel.tsx` rather than mounting two competing implementations. The original conflicting cash-gap task handoff stays main's version; source commits remain preserved. The user's merge instruction permits these existing PR conflict/integration corrections, including the incoming controls component noted in the original handoff.

Review corrections needed before acceptance: derive displayed result/cash-diagnostic domain counts from their own bounds, since parsed huge JSON integers can round; make the large-count regression actually exceed MAX_SAFE_INTEGER; reject invalid dates/amounts/targets and display no valid count for invalid drafts. Keep all financial arithmetic on the backend and server proof states authoritative. Do not expose implementation placeholders in the product. Failure dates are labelled as belonging to the selected witness, not an unsupported global earliest-cash claim.

Stage 1 prerequisite is now merged: PR36 at `8f6c627e9d825a07630658d6d906e2772690a6b0`. Incorporated that main normally and adapted its one approval selector. The final tree has exactly one cash-gap panel and preserves main's prior proof/authorization/state regressions.

Validation on Node 22.23.2/npm 10.9.8/Python 3.12: locked `npm ci` installed 439 packages; typecheck, lint and production build passed (156 kB first-load JS). Full `npm run test:e2e` on isolated ports 8148/3148 ran 46 cases: 45 passed in 3.3 minutes and one old assertion expected the replaced global-earliest label. Updated that assertion to the witness-date label and reran its complete workflow: 1 passed in 22.1 seconds. All 46 cases are covered by those passing results. The final complete rerun is also required in CI before merge.

The real-API precision regression preserves exact `29121790002912179` in preflight, the one-case incomplete verification and its cash diagnostic; converting that count to a Number demonstrably rounds it. Gregorian cases include DST, leap/non-leap centuries, years below 100 and invalid year zero. Invalid amount/historical-date drafts, duplicates and dimension limits are covered. The 390px keyboard screenshot and large-count result screenshot were inspected; layout is readable. Existing history, privacy/deletion, consent, review, workspace, cash-gap and verification checks passed. Ownership/current-main and whitespace checks pass. No new synthesis, consequence feature, provider call or human rehearsal is part of this task.
