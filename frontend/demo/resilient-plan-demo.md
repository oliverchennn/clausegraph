# Resilient schedule segment — implemented synthetic demonstration

Engine/API/fixture PR41 (`c420609`) and comparison/adoption PR42 (`b53cb5c`) are merged. This segment replaces the older **proposed synthesis** description in the baseline C10/C11 materials; C15 will consolidate the full script after the consequence stage. All documents and amounts here are fictional. No provider call, real approval, payment or cancellation occurs.

The 30-second beat below is a proposed presenter budget, not a measured human run. The browser regression checks behavior without pretending its execution time is spoken delivery.

## The transition from the original example

Keep the original six-source example and its September 21–28 bounds. Press **Find verified alternative**. It reports **No solution in this declared domain**. Explain the scope: recorded permissions, saved nominal assumptions, allowed action/date schedules and this horizon. It does not rule out future approvals, other actions, extra cash or adaptive policies.

Do not shrink those bounds to claim success. Say: “This example has no permitted schedule that survives that range. Here is a separate synthetic example where a different fixed schedule helps.” Press **Open resilient example**. The banner explicitly identifies its **three fixture documents**. This opens a separate session; it does not revise the original problem into a success.

## The proposed 30-second success beat

1. Press **Payday Sep 3–5**, then **Verify fixed plan**. The saved nominal schedule uses the already-approved September 3 payment option with no fee. Its nominal minimum is **$50**, but it is **Unsafe** when the paycheck is delayed. The complete worst minimum is **−$50**.
2. Press **Find verified alternative** with those same bounds. The comparison selects the already-approved September 5 payment option. Its **$1** fee leaves **$49** as both nominal and proven worst minimum. The backend's independent fixed-plan verifier reports **SAFE, 3/3 cases, complete coverage**.
3. Point to the fixed execution dates: both options must be executed on **September 1**. The changed option moves the same **$100** installment to a different payment date. No extra cash, permission, duplicate debt or per-outcome reoptimization was introduced. The first verified feasible schedule is returned; no optimality or minimum-fee claim is made.
4. Open **Evidence for late-shift** to show the approved date and fee in the exact source quote. Close the drawer. Until now the saved plan and history are unchanged.
5. Press **Adopt verified schedule** explicitly. The API reconstructs and rechecks that tuple, then saves the plan and its proof together. The dashboard shows **$49** and **Verified Safe**; history retains the old nominal plan and the new schedule/proof. Adoption executes nothing externally.

The displayed horizon is September 1 inclusive through September 8 exclusive. Every date September 3, 4, 5 is checked. Bounds are user assumptions, not probabilities or promises extracted from the paycheck notice. Approval is recorded independently from source support and fixture review.

## Honest alternatives to the success headline

| Observed result | Presenter statement |
|---|---|
| Verified alternative found | “This one fixed schedule passes the saved nominal case and every declared uncertainty case. We have not proved it is the best schedule.” |
| No solution in this declared domain | “Every admissible tuple was refuted in this specific model; we did not establish impossibility outside it.” |
| Search inconclusive | “The budget or unresolved facts prevented a conclusion. No verified candidate is available, and no-solution has not been proved.” |
| Adoption rejects changed inputs or times out | “Reload the current saved plan before retrying. A preview is not a stored approval or proof credential.” |

Never relabel an incomplete observed minimum as the worst case. A refutation example explains one tuple; the counters and exhaustive status establish a domain-wide no-solution result. A pending or denied recorded permission cannot be made usable by a hypothetical singleton-approved uncertainty outcome.

## Local fallback

With the pinned backend environment, run:

```bash
python scripts/synthesize_demo.py
```

Save stdout as UTF-8 JSON under an ignored local path such as `.data/fallback/synthesis.json`. The script makes zero external calls and performs no adoption. It asserts the nominal early option fails unchanged September 3–5 bounds and the fixed late option passes, with nominal/worst minimum 4900 cents and fee 100 cents. IDs/timestamps/runtimes vary; financial/proof claims must match the current report.

Keep `python scripts/verify_demo.py` for the original eight-date impossibility and 40000-cent hypothetical cash diagnostic. These reports demonstrate different problems. If the app is unavailable, call the files **previously computed synthetic results**, not a live browser run or a successful provider extraction. Never substitute a saved SAFE label for an inconclusive current request.

The real-API `resilient-plan-demo.spec.ts` follows this transition, preserves the original session, checks unchanged bounds and fixed actions, opens evidence, requires explicit adoption, and verifies the saved proof after reload. Component/error/cutoff/keyboard/mobile regressions remain in `resilient-plan.spec.ts`. Successful live extraction and human spoken delivery remain separately unverified.
