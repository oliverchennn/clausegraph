# A merge coordination: presenter kit

The user requested integration of current PRs, then specifically required checking whether each was useful or superseded. This is A's integration record for B-lane PR37, not a replacement of C's original handoff. C's original files and history are preserved.

Starting head: `d99c16fbf304b90428ea9a21b7678c64c076052c`. Merged current main `c00d39573464d916ea3712c42a766df0a791fb0a` normally in isolated `.worktrees/dev-a-merge-demo-kit`, without conflicts. Allowed correction: `frontend/demo/presenter-cues.md` and this record. All other application files remain current main's versions.

The three presenter documents are new and useful; main has no equivalent kit. Their historical `8fa1279` capability table remains explicitly dated. The cash-gap UI is now delivered by PR34; future synthesis/walkthrough beats remain proposed, and human rehearsal remains unperformed. Corrected two sentences so a cutoff with a concrete failure stays UNSAFE rather than being described as necessarily UNKNOWN.

Validation: 9 relative links resolve; all four fallback script paths and the reset flag exist. Re-executed `verify_demo.py`: nominal 5000 cents, UNSAFE 8/8, September 26 shortfall -40000 cents, impossibility certificate true, and the same schedule SAFE with 40000 hypothetical cents. `eval_nemotron.py`: 5 cases, 3 exact fields, 3 valid citations, all 5 unreviewed candidates withheld, no model accuracy measured. Ownership/current-main and whitespace checks pass. Full CI is required before merge and its exact result will be recorded in A's PR review. No providers, human rehearsal or new feature implementation are part of this integration.
