# Developer C: frontend and demo delivery

The user's 2026-09-20 reassignment gives C approximately **30% of remaining required work**, alongside A's 35% and B's 35%. C now implements assigned frontend features and owns demo preparation. The old C1-C9 review queue, two/three-prompt budget, 20-45-minute limits and two-file cleanup cap are retired. Previous assignments are complete per the user; PR26/27 still need integration as recorded in [WORKSTREAMS](WORKSTREAMS.md). Do not repeat them.

## Start and ownership

Read [AGENTS](../AGENTS.md), [WORKSTREAMS](WORKSTREAMS.md), [HACKATHON_MVP](HACKATHON_MVP.md), [HACKATHON_ASSIGNMENTS](HACKATHON_ASSIGNMENTS.md), [RESUME](RESUME.md), relevant merged handoffs and [verification limits](handoffs/verification.md). Synchronize safely yourself: inspect status/worktrees and PRs, fetch with pruning, preserve dirty/divergent work, and use fresh fetched main. Record actual starting and prerequisite merge SHAs, never a peer's unmerged head.

The checker still has two lanes. C works as **Contributor C; ownership lane dev-b; B remains frontend owner**, using `codex/dev-b/c-<task>`, `.worktrees/dev-c-<task>` and C's own `docs/handoffs/dev-b/c-<task>.md`. No dev-c branch, checker change or guard bypass is needed. A owns schemas/generated types, dependencies/locks, backend/fixtures/scripts, shared docs and merge coordination. C's demo sources live under already B-owned `frontend/demo/`; A incorporates accepted summaries into central docs.

This merged assignment explicitly delegates the **new exact paths below**. C can create them once task prerequisites are met without another tiny-fix authorization. Existing shared files require B's committed exact-path release on main. No blanket frontend permission or peer handoff edits. B avoids C's assigned new files while active. One implementation agent/task per developer; no B/C implementation agents spawned from A's session. Stop after each task's handoff/PR, then start the next eligible task in a fresh worktree.

## Required queue and exact paths

Points estimate relative effort, not hours or promised throughput. These six tasks total **30 points**. Each also permits C's matching handoff; no other files are implicitly allowed.

| Task / effort | Prerequisite and exact allowed files | Deliverable and acceptance |
|---|---|---|
| **C10 `c-demo-kit` / 6** | This reassignment merged; use the merged baseline without waiting for PR25/28. New `frontend/demo/presenter-cues.md`, `frontend/demo/fallback-runbook.md`, `frontend/demo/rehearsal-log.md`. | Six-beat proposed 180-second script, judge questions/answers, exact local fallback/reset commands from existing docs, and rehearsal record template. Distinguish runnable controls from proposed beats. Label saved outputs synthetic/previously computed; record source SHAs and untested limits. Prepare human rehearsal and log actual timing only with a presenter. No new setup scripts/live calls. |
| **C11 `c-cash-gap-demo` / 2** | Stage 0 local gate and stage 1 A/B implementation merged. New `frontend/demo/cash-gap-demo.md`, `frontend/tests/cash-gap-demo.spec.ts`. | Presenter segment and focused real-API demo regression: failure -> hypothetical cash explanation -> evidence -> unchanged saved plan/reload. Check proof-qualified versus incomplete labels; explain cash cannot repair denied approval. Reuse merged results/fixtures; no frontend arithmetic or duplicate backend oracle. |
| **C12 `c-uncertainty-failure-view` / 5** | Stage 1 accepted; A's stage 2 data contract and B's controls/state merged. New `frontend/src/components/uncertainty-failure-view.tsx`, `frontend/tests/uncertainty-failure-view.spec.ts`. B releases only import/render/callback wiring in `frontend/src/components/verify-plan.tsx` in B's merged handoff. | Accessible bounded failure table/details, evaluated/unchecked coverage labels and evidence navigation. B supplies typed result/selection callbacks; C renders backend counterexamples, including failures with no permitted cash trace. Wire the view through the released section; no form/request-state rewrite. Test Safe/Unsafe/Unknown, keyboard/mobile, stale-result clearing through B's state, and exact or qualified large counts. |
| **C13 `c-resilient-plan-demo` / 3** | Stage 2 accepted; reviewed synthesis design and stage 3 A/B implementation merged. New `frontend/demo/resilient-plan-demo.md`, `frontend/tests/resilient-plan-demo.spec.ts`. | Prepare/test the synthetic nominal-fails/resilient-survives story, unchanged bounds, fixed action/date comparison, explicit adoption and saved-plan preservation until adoption. Include inconclusive/no-solution fallback. Use A's separately labeled fixture and independent verification; no new search/arithmetic or claim the original fixture admits a resilient schedule. |
| **C14 `c-consequence-walkthrough` / 10** | Stage 3 accepted; A's stage 4 provenance contract and B's graph/evidence hooks merged. New `frontend/src/components/consequence-walkthrough.tsx`, `frontend/src/components/consequence-steps.tsx`, `frontend/tests/consequence-walkthrough.spec.ts`, `frontend/demo/consequence-demo.md`. B releases only import/mount/callback wiring in `frontend/src/app/page.tsx` in B's merged handoff. | Own source -> reviewed rule -> dependency -> effect -> cash walkthrough, keyboard steps and mobile/list view. Mount through B's released section; use existing graph/evidence callbacks and backend previews. Show removed charges, accelerated existing debt, fees/blockers and future obligations. Real-API tests cover preview nonmutation, exact evidence links, blocked states and established cancellation values without duplicated debt. |
| **C15 `c-final-demo` / 4** | Stage 4 A/B/C14 implementation integrated and required application checks pass; C15 completes its presentation gate. Update C10's three demo files and `frontend/demo/consequence-demo.md`; new `frontend/tests/demo-story.spec.ts`. | Assemble judge story and integrated browser rehearsal at desktop/390px with keyboard; preserve saved synthetic fallback and recovery. Facilitate/record human spoken run when available; distinguish automated timing. Report defects with owners and outstanding blockers. A updates central delivery/demo claims. No unassigned UI polish sweep. |

C10 proceeds alongside A/B closeout. C11/C13 are demo tasks; C12/C14 are implementation tasks. Stage 2/4 gates include C's assigned views; final presentation includes C15. C's extra review remains optional and never replaces required A/B mutual review. If C is unavailable, A records a narrow reassignment; do not silently waive deliverables or send B into C's active files.

## Interfaces, release and handback

Before C12/C14, B records exact props/callbacks, stable selectors, permitted wiring section and release SHA in B's own handoff on main. A's merged contracts supply financial/proof/provenance data. C records those SHAs before editing. C owns its assigned new component/test paths; B retains state/forms/API client, graph and evidence primitives. Request schema/dependency changes from A; never invent fallback financial values or edit generated types.

Existing-file releases expire on task merge, recorded B reclaim, invalidated prerequisite interface or overlapping work. C stops the overlapping edit, preserves the diff and reports handback; A/B record reassignment before that scope resumes. Unrelated main updates may be merged normally with affected checks. B may reclaim promptly, but required work is explicitly reassigned, not dropped. No reply or unmerged release grants shared-file permission.

## Checks and limits

C10's Markdown-only checks cover links, commands against merged source, claim/status labels, proposed timing totals, whitespace, ownership and handoff. No full application rerun for prose alone. C11-C15 code/test changes use Python3.12, Node22.23.2/npm10.9.8 and locked dependencies; run relevant real-API browser tests, typecheck/lint/build, desktop/390px and keyboard checks as applicable, plus normal full CI before merge. Record exact commands/results and unrun checks. C validates C's changes; B owns shared integration/regressions; A owns financial/API validation and reviews all B-lane patches.

Use isolated synthetic sessions, worktree-local storage and distinct API/web ports, never another developer's services or `.next`. No provider calls, cloud provisioning, private real-data access, payments/cancellations/applications/messages are authorized. Presenter facilitation means preparing materials and recording a run in this task, not sending invitations/messages. Missing human availability stays explicit and does not block independent local engineering.

## Copyable next prompt

> You are Developer C on ClauseGraph, now assigned approximately 30% of remaining work, mostly frontend/demo. Read AGENTS.md and docs/DEV_C.md with required coordination docs. Safely synchronize and confirm this reassignment is on main. Start C10 `c-demo-kit` only in a fresh `codex/dev-b/c-demo-kit` branch/worktree. Create only its three assigned demo Markdown files plus your own handoff. Use the merged baseline, preserve completed C work, label future controls proposed, prepare a six-beat 180-second script and usable synthetic fallback, and record actual versus unperformed rehearsal checks. Publish for A review/normal CI, then stop. No A/B implementation, central-doc edits, live calls or messages.
