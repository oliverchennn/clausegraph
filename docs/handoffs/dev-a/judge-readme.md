# A / solo orchestrator: judge-facing README

## Assignment before implementation

The user asks to update, push and merge a new README for judges. Root main is clean and synchronized with fetched `origin/main` at `9cbe7490adefbbd237c1a21327548808221665d6` (PR49). No existing modified README or open README PR was found. Fresh branch/worktree: `codex/dev-a/judge-readme`, `.worktrees/dev-a-judge-readme`. All earlier worktrees, including the interrupted comparison-visibility task, remain untouched.

Allowed paths: `README.md` and this handoff. This is documentation only: organize the product story and judge walkthrough around the delivered application, replace outdated deferred-feature/contributor claims, preserve setup instructions, and state bounded verification and provider limits accurately. Required references are the merged C15 presenter/fallback materials, canonical model configuration, verification and synthesis contracts, and actual CI. No application, dependency, policy, deployment or historical-handoff changes.

Acceptance: source-to-cash story, six implemented judge beats, exact synthetic cash/proof results, separate nominal optimization/fixed-plan verification/synthesis, model roles and consent, runnable local setup, valid relative links, evidence-backed validation summary. Same-agent orchestrator review is authorized and will be identified honestly. Push and merge only after the required ownership/current-main checks and full green PR CI.

## Evidence before editing

Current main CI run 35520190296 succeeded: 483 backend tests including PostgreSQL and 66 browser tests, Ruff, generated contract drift, frontend typecheck/lint/build, and clean installs on Linux/macOS/Windows. The README still calls synthesis deferred and assigns C an obsolete optional-only role. Merged PR49's presenter cues include cash diagnostics, separate resilient search/adoption and the consequence walkthrough. Provider model defaults are configuration values, not live-accuracy claims.

## Result and local validation

Rewrote the README around the user's judge audience: product impact, six-step synthetic walkthrough, three separate planning/verification/search operations, architecture diagram, exact Nemotron roles, Windows and macOS/Linux startup, offline fallback, and linked CI evidence. Removed obsolete deferred-feature and optional-only-C descriptions. The comparison panel's current location and the need to restart services after updating code are documented without claiming the interrupted UI fix is complete.

Same-agent orchestrator review checked financial values against the merged presenter/fallback sources, preserved original-domain no-solution versus separate-fixture success, and kept synthetic/live/provider/human-rehearsal claims distinct. No hosted site was inspected or deployed. Source code, dependencies, policies and user data are unchanged.

Local checks passed with Python 3.12: all 18 relative README links resolve; code fences are balanced; both model IDs match backend configuration; `verify_demo.py`, `synthesize_demo.py` and offline `eval_nemotron.py` exit successfully and produce valid JSON. `git diff --check` passed and the managed pre-push hook installed successfully. Full application checks will run in PR CI; exact head and results will be recorded on the PR before merge.
